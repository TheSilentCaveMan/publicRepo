using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class TestEntityMove : AIMove
{
    internal override void CallMove(GameObject target)
    {
        base.CallMove(target);
    }
    internal override void Move(Vector3 movePosition)
    {
        base.Move(movePosition);
    }
}
